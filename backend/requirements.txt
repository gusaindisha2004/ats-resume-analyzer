# ATS Resume Analyzer — backend dependencies.
#
#     pip install -r backend/requirements.txt
#     python -m spacy download en_core_web_md
#
# WeasyPrint needs system libraries on Linux:
#     sudo apt install -y libcairo2 libpango-1.0-0 libpangoft2-1.0-0 libffi-dev

# ── Web framework ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
python-dotenv>=1.0

# ── NLP / ML ──
spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6

# ── File parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27
python-magic-bin>=0.4.14; sys_platform == 'win32'   # bundles libmagic DLLs on Windows

# ── PDF report generation ──
jinja2>=3.1
weasyprint>=60.0

# ── External services ──
httpx>=0.27                  # async Supabase REST client
groq>=0.13
PyJWT[crypto]>=2.8           # verifies Supabase access tokens

# ── Dev / test ──
pytest>=8.0
pytest-asyncio>=0.23
